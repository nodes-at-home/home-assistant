###
### 15.12.2023 new
###

  - trigger:
      - platform: state
        entity_id: sensor.electricity_meter_quarterhourly
        attribute: last_reset
    sensor:
      - name: electricity_quarterhourly
        unit_of_measurement: kWh
        state: "{{ state_attr ( 'sensor.electricity_meter_quarterhourly', 'last_period' ) }}"
        attributes:
            last_reset: '{{ state_attr ( "sensor.electricity_meter_quarterhourly", "last_reset" ) }}'
