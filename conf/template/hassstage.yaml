###
### 09.11.2019 junand
###

  - binary_sensor:

      - name: prod_mode
        unique_id: prod_mode
        state: "{{ is_state ( 'input_text.hostname', 'nodesathome1' ) }}"

      - name: dev_mode
        unique_id: dev_mode
        state: "{{ not is_state ( 'input_text.hostname', 'nodesathome1' ) }}"
